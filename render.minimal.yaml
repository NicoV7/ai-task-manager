services:
  - type: pserv
    name: taskmanager-db
    plan: starter

  - type: web
    name: taskmanager-backend
    runtime: docker
    rootDir: ./backend
    dockerfilePath: ./Dockerfile.prod
    plan: starter